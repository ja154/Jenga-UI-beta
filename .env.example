# Copy this file to `.env.local` and fill in real values.

# Required: Gemini API key used by the backend server.
GEMINI_API_KEY=your_gemini_api_key_here

# Optional: Python binary used for /api/design-system.
# Keep default unless your environment uses a different executable.
PYTHON_BIN=python3
